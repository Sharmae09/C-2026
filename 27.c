#include <stdio.h>
int main(){
    int x, y;
    scanf("%d %d", &x, &y);
    int res =  (x * (y -1) +1);
    printf("%d", res);
    return 0;
}

// https://open.kattis.com/problems/faktor?editresubmit=18903647
// i forgot & again
