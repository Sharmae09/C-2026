#include <stdio.h>
int main(){
    
        int x, y;
    scanf("%d ", &x);
       scanf("%d ", &y);
        // printf("%d", x-(y*2));
        printf("%d", x%y);
    return 0;
}

// I used the wrong logic because I did not read the problem first
// https://open.kattis.com/problems/flatbokuveisla?editresubmit=18917632
